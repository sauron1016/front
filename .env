# API base used by browsers - must point at this machine's LAN address so
# clients outside the VM reach the backend (localhost would hit their own PC)
VITE_API_URL=https://backend-1-a54s.onrender.com
